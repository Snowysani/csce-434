main
var input;

function foo (x , y, z)
{
	call outputnum(x);
	call outputnum(y);

	if x > y then 
		call outputnum(9);
		return x;
	fi;
	return 7;
};

{
	call outputnum(call foo(5, 3));
	call outputnewline();
	call outputnum(8);
}
.