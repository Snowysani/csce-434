main
var input, myvar;
function getsumton(n)
var i,j;
{
    let i <- 0;
	let j <- 1;
    while j <= 3 do
		let i <- i + j; // 1 3 6
		let j <- j + 1; // 2 3 4
	od;
    return i;
};

{
    let input <- 3;
    call outputnum(input);
    call outputnewline();
    
    let myvar <- call getsumton(input);

    call outputnum(myvar);
    call outputnewline();

}.