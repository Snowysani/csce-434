main
var csum;

function cumulativesum()
var input, sum;
{	
	let input <- call inputnum();
	if input == 0 then
		return 0;
	else
        let sum <- call cumulativesum();
		return input + sum;
	fi;
};

{
	let csum <- call cumulativesum();
	call outputnum(csum);
}.